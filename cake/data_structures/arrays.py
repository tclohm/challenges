"""

RAM is basically an array already

arrays have fast lookups (O(1) time), but each in the array needs to be
the same size, and you need a big block of uninterrupted free memory to
store the array

"""